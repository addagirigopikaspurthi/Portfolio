# Addagiri Spoorthi — Portfolio

Static personal portfolio. No build step, no dependencies.

## Files (keep these at the ROOT of the repo)
- index.html
- vercel.json

## Deploy
Vercel -> Add New -> Project -> import this repo.
Framework Preset: Other. Build Command: (empty). Output Directory: (empty).

IMPORTANT: index.html must be at the repository ROOT, not inside a folder.
